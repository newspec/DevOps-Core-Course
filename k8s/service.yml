apiVersion: v1
kind: Service
metadata:
  name: python-app-svc
  labels:
    app: python-app
    component: web
spec:
  type: NodePort
  selector:
    app: python-app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30080
